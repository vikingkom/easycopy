version: '3.8'

services:
  easycopy-server:
    build:
      context: ..
      dockerfile: server/Dockerfile
    ports:
      - "${EASYCOPY_PORT:-8000}:8000"
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - TZ=${TZ:-UTC}
    container_name: easycopy-server
